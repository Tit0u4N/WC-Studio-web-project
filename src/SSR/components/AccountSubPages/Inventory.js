import {AccountSubPageCSSClasses} from "../../layouts/layoutSubPage.js";


export const Inventory = (id = "Inventory") => {
    return `
        <div class="${AccountSubPageCSSClasses}">
            <h1>${id}</h1>
        </div>
`
}